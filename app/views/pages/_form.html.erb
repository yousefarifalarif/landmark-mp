<%= simple_form_for(@landmark) do |f| %>
  <%= f.input :name, label: "Landmark Name" %>
  <%= f.input :location, label: "Location" %>
  <%= f.input :date, label: "Choose a date" %>
  <%= f.submit %>
<% end %>
