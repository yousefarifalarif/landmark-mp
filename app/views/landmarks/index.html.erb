<div class="container">
  <div id="title">
    <h1>Landmarks</h1>
  </div>
  <div class="cards">
    <%@landmarks.each do |landmark|%>
    <div class="card-trip">
    <%= link_to landmark_path(landmark), class:"card-trip" do %>
      <%= cl_image_tag landmark.photo.key %>
      <div class="card-trip-infos">
        <div>
          <h2><%= landmark.name %></h2>
          <p><%= landmark.location %></p>
        </div>
        <h3><%= humanized_money_with_symbol(landmark.price) %></h3>
      </div>
      <% end %>
    </div>
    <%end%>
  </div>
</div>
